node_modules
.pnpm-store
.turbo
.next
dist
build
coverage
# TypeScript incremental build state. Only the web app sets `incremental: true`,
# so `pnpm typecheck` regenerates its copy while `pnpm build` does not. Tracking
# it only produces meaningless diffs and cross-branch conflicts.
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
apps/mobile/.expo
apps/mobile/dist
packages/db/prisma/dev.db
DiazMartialArts/
