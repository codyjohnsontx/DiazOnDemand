node_modules
.pnpm-store
.turbo
.next
dist
build
coverage
# TypeScript incremental build state. Regenerated by every build, so tracking it
# only produces meaningless diffs and cross-branch conflicts.
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
apps/mobile/.expo
apps/mobile/dist
packages/db/prisma/dev.db
DiazMartialArts/
